using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class PhysicsNode : Node
{
    public PhysicsNode(ToolWindow _toolWindow, Vector2 spawnPos) : base(_toolWindow)
    {
        toolWindow = _toolWindow;

        nodeWidth = 100;
        nodeHeight = 100;

        NodeUnscaledPosition = new Rect(spawnPos.x, spawnPos.y, nodeWidth, nodeHeight);

        color = Color.grey;
    }
}
*/
