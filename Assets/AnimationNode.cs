using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*

public class AnimationNode : Node
{
    public AnimationClip clip;
    public bool m_bLoop = false;

    public AnimationNode(ToolWindow _toolWindow, Vector2 spawnPos) : base(_toolWindow)
    {
        toolWindow = _toolWindow;

        nodeWidth = 150;
        nodeHeight = 50;

        NodeUnscaledPosition = new Rect(nodeWidth, nodeHeight, nodeWidth * toolWindow.scale, nodeHeight * toolWindow.scale);

        color = Color.white;
    }
}
*/
