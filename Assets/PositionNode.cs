using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class PositionNode : Node
{

    public PositionNode(ToolWindow _toolWindow, Vector2 spawnPos) : base(_toolWindow)
    {
        toolWindow = _toolWindow;

        nodeWidth = 50;
        nodeHeight = 50;

        NodeUnscaledPosition = new Rect(spawnPos.x, spawnPos.y, nodeWidth, nodeHeight);

        color = Color.cyan;
    }
}
*/